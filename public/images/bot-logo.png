
const fs = require('fs');
const path = require('path');

// ─── [ PUBLIC CDN LOGIC ] ──────────────
const botLogoPath = path.join(__dirname, '../Public/images/bot-logo.png');
const botLogo = fs.existsSync(botLogoPath) ? fs.readFileSync(botLogoPath) : null;





